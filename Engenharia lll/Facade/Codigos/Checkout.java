class Checkout {
    void efetuarCheckout(int pedidoId, double totalPreco) {
        System.out.println("Processando o pagamento do pedido #" + pedidoId + " no valor de R$ " + totalPreco);
    }
}